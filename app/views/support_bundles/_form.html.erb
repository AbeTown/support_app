<%= form_for(@support_bundle) do |f| %>
  <% if @support_bundle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@support_bundle.errors.count, "error") %> prohibited this support_bundle from being saved:</h2>

      <ul>
      <% @support_bundle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
